# Disable WiFi power saving — prevents the adapter from going silent under low traffic.
# Install: sudo cp wifi-powersave-off.conf /etc/NetworkManager/conf.d/
#          sudo systemctl restart NetworkManager
[connection]
wifi.powersave = 2
